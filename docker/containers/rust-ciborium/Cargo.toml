[package]
name = "cbor-test-server"
version = "1.0.0"
edition = "2021"

[dependencies]
ciborium = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
hex = "0.4"
tiny_http = "0.12"

[profile.release]
opt-level = 3
lto = true
